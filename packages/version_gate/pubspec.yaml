name: version_gate
description: Production-ready Version Gating, SemVer comparison, and Anti-Stale Cache manager for Constellation apps.
version: 1.0.0
homepage: https://github.com/constellation/version_gate
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  meta: ^1.11.0

dev_dependencies:
  test: ^1.24.0
